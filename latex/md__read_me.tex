
\begin{DoxyCode}{0}
\DoxyCodeLine{``` git clone https://github.com/bud-\/ea/exam\_Tech\_prog\_summer\_2021``` — клонирование репозитория}
\DoxyCodeLine{}
\DoxyCodeLine{```git branch 201-\/331\_Skrypnik``` — создание ветки}
\DoxyCodeLine{\_\_\_}
\DoxyCodeLine{\#\#\#\# Переключение веток}
\DoxyCodeLine{}
\DoxyCodeLine{```git checkout 201-\/331\_Skrypnik}

\end{DoxyCode}


{\ttfamily git checkout master} \DoxyHorRuler{0}


{\ttfamily git branch} — список веток и отображение текущей

{\ttfamily git add -\/-\/all} — индексирование новых, измененных, удаленных файлов

{\ttfamily git commit -\/m \char`\"{}сообщение коммита\char`\"{}} — фиксация в коммите файлов добавленных командой git add

{\ttfamily git merge 201-\/331\+\_\+\+Skrypnik} — слияние активной ветки с указанной

{\ttfamily git status} -\/ статус репозитория

{\ttfamily git tag -\/a -\/m \textquotesingle{}Message\textquotesingle{} v1.\+0 master} — создать тег с сообщением на активном коммите ветки master

{\ttfamily git push -\/u origin master} — пушим в удаленный репозиторий ветку master 